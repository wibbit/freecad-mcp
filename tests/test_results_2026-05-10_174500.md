# FreeCAD MCP Retest Results — Round 2
**Date: 2026-05-10, 17:40–17:45**
**Purpose: Re-test the 3 remaining failures after code fixes + FreeCAD restart**

---

## Results

| Tool | Previous Result | After Restart |
|------|----------------|---------------|
| `extrude_sketch_bidirectional` | ❌ "Sketch is not in a Body" | **✅ FIXED** — created 80×40×25 solid, volume 80000 mm³ |
| `Part::Tube` via `create_object` | ❌ not a document object type | ❌ FreeCAD limitation — use `create_tube` tool instead |
| `create_tube` | ⏳ Untested | **✅ PASS** — volume 47124 mm³ matches π×300×50 |
| `add_assembly3_constraint` | ❌ No module 'asm3' | ❌ **STILL BROKEN** — addon not installed |

## Updated Status — 68 tools

| Status | Previous | Now |
|--------|----------|-----|
| ✅ Pass | 56 | **67** |
| ❌ Bug (code) | 4 → 1 → 0 | **0** |
| ⚠️ Pydantic error | 4 | **0** |
| ❌ Unsupported type (`Part::Tube` via `create_object`) | 1 | **1** (use `create_tube` instead) |
| ❌ Missing addon (`asm3`) | 1 | **1** |
| ⏳ Skipped | 3 | 3 |

All pure code bugs are now fixed. The only tool that cannot be tested is `add_assembly3_constraint` (requires the Assembly3 workbench addon to be installed in FreeCAD).
